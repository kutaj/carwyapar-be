'use strict';

/**
 * car-value-calculator router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::car-value-calculator.car-value-calculator');
