'use strict';

/**
 * used-car-dealer-ship router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::used-car-dealer-ship.used-car-dealer-ship');
