'use strict';

/**
 * used-car-dealer-ship controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::used-car-dealer-ship.used-car-dealer-ship');
