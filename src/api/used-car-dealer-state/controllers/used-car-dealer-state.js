'use strict';

/**
 * used-car-dealer-state controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::used-car-dealer-state.used-car-dealer-state');
